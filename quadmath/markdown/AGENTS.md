# markdown/ — Agent Notes

Manuscript section sources, `00_preamble.md` … `10_symbols_glossary.md` (re-verified 2026-08-31; section table in the existing README.md). Processed by
`../scripts/render_pdf.sh` into `../output/tex/` and `../output/pdf/`.
Editing rules: one section per file; cross-references and equations must
validate — run `../scripts/validate_markdown.py` after edits.
