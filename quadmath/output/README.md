# output/

Generated manuscript build artifacts: figure data (`data/`), figures (`figures/`),
LaTeX (`tex/`, `latex_temp/`), PDFs (`pdf/`), and the combined review markdown.
Regenerate with `../scripts/render_pdf.sh`; clean with `../scripts/clean_output.sh`.
