# output/ — Agent Notes

All generated build products (verified 2026-08-29): `data/` (CSV/NPZ figure
data), `figures/` (PNG figures), `tex/` (per-section LaTeX),
`latex_temp/` (LaTeX intermediates), `pdf/` (per-section PDFs), plus the
combined `quadmath_review.md`. Everything here is regenerable via
`../scripts/render_pdf.sh` and safe to delete (`../scripts/clean_output.sh`).
Never hand-edit.
